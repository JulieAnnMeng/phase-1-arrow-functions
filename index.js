const divide = () => 2000 / 100;
//The test does not add in any parameters for the test to pass. I would write it as follows
// const divide = (parameter1, parameter2) => parameter1 / parameter2;
const square = x => x ** 2;

const add = (parameter1, parameter2) => parameter1 + parameter2;